# ─── MongoDB ──────────────────────────────────────────────
# Connection string. Use mongodb+srv://... for Atlas.
MONGODB_URI=mongodb+srv://admin:THS_Code123@cluster0.f0upd.mongodb.net
MONGODB_DB="fe-conclave"

# ─── SMTP (Nodemailer) ────────────────────────────────────
# Generic SMTP. Works for Gmail (with app password), Mailgun, SES, etc.
# Gmail example:
#   SMTP_HOST=smtp.gmail.com
#   SMTP_PORT=587
#   SMTP_USER=you@gmail.com
#   SMTP_PASS=<16-char app password from https://myaccount.google.com/apppasswords>
#   MAIL_FROM="Future Engineers Conclave <you@gmail.com>"
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=team@code.in
SMTP_PASS=bbsywmhaajktphjx
MAIL_FROM=team@code.in

# ─── Admin auth ───────────────────────────────────────────
# Bootstrap admin (always works, even if MongoDB is empty).
# Once logged in you can add more admins from /admin/admins.
ADMIN_EMAIL=team@code.in
ADMIN_PASSWORD=bbsywmhaajktphjx

# Random secret for signing the admin session cookie. Min 16 chars.
# Generate with:  openssl rand -base64 32
ADMIN_SESSION_SECRET=""

# ─── Public env (used by site) ────────────────────────────
NEXT_PUBLIC_WHATSAPP_NUMBER="+919959682957"
NEXT_PUBLIC_EVENT_START_ISO="2026-05-23T18:00:00+05:30"
NEXT_PUBLIC_EVENT_END_ISO="2026-05-23T20:30:00+05:30"
